# HOW TO RUN

```bash
# config environment
create .env file

# run mysql using docker or local mysql instead.
docker-compose up -d

# start app
npm install
npm start

# migrations and undo-migrations
npm run migrate
npm run undo-migrate

# seed and undo-seed
npm run seed
npm run undo-seed

```
